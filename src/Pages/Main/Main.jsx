import React from 'react';
import styled from '@emotion/styled';

const Layout = styled('div')({
  display: 'flex',
  flexDirection: 'column',
  height: '100%',
});

const Main = () => (
  <Layout>
    Here is layout
  </Layout>
);


export default Main;
